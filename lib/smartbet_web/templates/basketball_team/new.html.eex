<h1>New Basketball team</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.basketball_team_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.basketball_team_path(@conn, :index) %></span>
